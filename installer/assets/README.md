# Installer Assets

Place the signed production `carthage-pos.ico` file in this directory before a
release build. The build remains functional with the Windows default executable
icon when the asset is absent; Inno Setup includes it automatically when present.
